workflows:
  ios-ad-hoc:
    name: iOS Ad Hoc Build (Deployment Target Force)
    max_build_duration: 60
    instance_type: mac_mini_m1
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - name: Prepare Flutter Environment
        script: |
          flutter precache --ios
          flutter pub get
        
      - name: Hard-Override Deployment Target
        script: |
          # 1. حذف المخلفات القديمة والروابط الرمزية تماماً
          rm -rf ios/Pods ios/Podfile.lock ios/.symlinks
          
          # 2. البحث عن أي ذكر لإصدار iOS قديم في كامل المشروع واستبداله بـ 13.0
          # يشمل ملفات Xcode وملفات الـ podspec الخاصة بالـ Plugins
          find . -type f -name "*.pbxproj" -exec sed -i '' 's/IPHONEOS_DEPLOYMENT_TARGET = [0-9.]*/IPHONEOS_DEPLOYMENT_TARGET = 13.0/g' {} +
          find . -type f -name "*.podspec" -exec sed -i '' 's/[.]ios[.]deployment_target.*/.ios.deployment_target = "13.0"/g' {} +
          find . -type f -name "*.podspec" -exec sed -i '' "s/'deployment_target' => '[0-9.]*'/'deployment_target' => '13.0'/g" {} +
          
          # 3. خدعة الـ Configuration لتجاوز قيود Flutter CLI
          sed -i '' 's/Release-production/Release/g' ios/Runner.xcodeproj/project.pbxproj
          
          # 4. توليد ملفات الربط (بإصدار 13.0 المجبور الآن)
          flutter build ios --release --no-codesign --pub
          
          # 5. إعادة التسمية للأصل لضمان عمل xcodebuild
          sed -i '' 's/Release/Release-production/g' ios/Runner.xcodeproj/project.pbxproj

      - name: Reconstruct Podfile
        script: |
          cd ios
          rm -f Podfile
          cat > Podfile <<EOF
          platform :ios, '13.0'

          project 'Runner', {
            'Debug-production' => :debug,
            'Release-production' => :release,
            'Debug-development' => :debug,
            'Release-development' => :release,
            'Debug' => :debug,
            'Profile' => :release,
            'Release' => :release,
          }

          load File.join('$FLUTTER_ROOT', 'packages', 'flutter_tools', 'bin', 'podhelper.rb')

          target 'Runner' do
            use_frameworks!
            use_modular_headers!
            flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
            pod 'PhoneNumberKit', '~> 3.7.6'
          end

          post_install do |installer|
            installer.pods_project.targets.each do |target|
              flutter_additional_ios_build_settings(target)
              target.build_configurations.each do |config|
                config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'
                config.build_settings['ENABLE_BITCODE'] = 'NO'
              end
            end
          end
          EOF

      - name: Update CocoaPods (Clean Install)
        script: |
          cd ios
          # تنظيف الكاش الخاص بـ CocoaPods على السيرفر تماماً
          pod cache clean --all
          pod install --repo-update
          
      - name: Create ExportOptions.plist
        script: |
          cat > ios/ExportOptions.plist <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>method</key>
              <string>ad-hoc</string>
              <key>signingStyle</key>
              <string>manual</string>
              <key>compileBitcode</key>
              <false/>
          </dict>
          </plist>
          EOF
          
      - name: Manual Xcode Build & Export
        script: |
          cd ios
          xcodebuild archive \
                     -workspace Runner.xcworkspace \
                     -scheme Runner \
                     -configuration "Release-production" \
                     -archivePath build/Runner.xcarchive \
                     CODE_SIGNING_ALLOWED=NO \
                     CODE_SIGNING_REQUIRED=NO \
                     CODE_SIGN_IDENTITY="" \
                     PROVISIONING_PROFILE="" \
                     IPHONEOS_DEPLOYMENT_TARGET=13.0

          xcodebuild -exportArchive \
                     -archivePath build/Runner.xcarchive \
                     -exportPath build/ipa \
                     -exportOptionsPlist ExportOptions.plist \
                     CODE_SIGNING_ALLOWED=NO
                     
    artifacts:
      - ios/build/ipa/*.ipa
    publishing:
      email:
        recipients:
          - alhajamekh@gmail.com