# 🔴 مشكلة خطأ الاشتراك في الباقات (Error 500)

## المشكلة:
عند محاولة الاشتراك في باقة، يظهر الخطأ التالي:

```
❌ Subscribe Package DioException
📄 Status Code: 500
Route [site.index] not defined
```

---

## السبب:
**المشكلة في الخادم (Backend)** وليس في التطبيق.

الخادم يحاول الوصول إلى route غير موجود:
```php
Route [site.index] not defined
```

الخطأ يحدث في:
```
/var/www/html/ymtaz/backend/app/Http/Tasks/Merged/Packages/SubscribePackageTask.php(61)
```

---

## الحل المطلوب من مطور Backend:

### 1. التحقق من ملف Routes
يجب التأكد من وجود route باسم `site.index` في ملف routes:

```php
// في ملف routes/web.php أو routes/api.php
Route::get('/', function () {
    return view('welcome');
})->name('site.index');
```

### 2. التحقق من ملف SubscribePackageTask.php
في السطر 61 من الملف:
```
/var/www/html/ymtaz/backend/app/Http/Tasks/Merged/Packages/SubscribePackageTask.php
```

يجب التأكد من أن الكود لا يستخدم `route('site.index')` بدون التأكد من وجوده، أو استبداله بـ URL مباشر.

### 3. الحل المؤقت:
إضافة route في ملف `routes/web.php`:
```php
Route::get('/', function () {
    return redirect('/home');
})->name('site.index');
```

---

## ما تم عمله في التطبيق:

### ✅ تحسين معالجة الأخطاء
تم تحديث التطبيق لعرض رسالة واضحة للمستخدم:

**قبل:**
```
حدث خطأ أثناء الاشتراك
```

**بعد:**
```
خطأ في الخادم. الرجاء المحاولة لاحقاً أو التواصل مع الدعم الفني
```

### ✅ معالجة صفحات HTML الخطأ
التطبيق الآن يتعرف على صفحات HTML الخطأ ويعرض رسالة مناسبة بدلاً من عرض كود HTML.

---

## كيفية الاختبار بعد إصلاح Backend:

1. تأكد من إصلاح route في Backend
2. أعد تشغيل التطبيق
3. حاول الاشتراك في باقة
4. راقب السجلات في Console:
   ```
   📦 Attempting to subscribe to package: [ID]
   🔑 Token: ...
   ✅ Subscribe Package Success
   ```

---

## رسائل الخطأ المحسنة:

| الحالة | الرسالة |
|--------|---------|
| خطأ 500 | خطأ في الخادم. الرجاء المحاولة لاحقاً أو التواصل مع الدعم الفني |
| صفحة HTML خطأ | خطأ في الخادم. الرجاء المحاولة لاحقاً |
| خطأ عام | حدث خطأ أثناء الاشتراك |
| Token غير موجود | الرجاء تسجيل الدخول أولاً |

---

## ملاحظة مهمة:
**التطبيق يعمل بشكل صحيح.** المشكلة في Backend ويجب على مطور الخادم إصلاحها.

السجلات التفصيلية في التطبيق ستساعد في تتبع أي مشاكل مستقبلية.
